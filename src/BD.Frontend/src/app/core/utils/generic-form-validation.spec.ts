import { GenericFormValidation } from './generic-form-validation';

describe('GenericFormValidation', () => {
  it('should create an instance', () => {
    expect(new GenericFormValidation()).toBeTruthy();
  });
});
